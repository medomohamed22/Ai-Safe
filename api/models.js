import{cors}from'./_lib.js';export default function handler(req,res){if(cors(req,res))return;if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});const raw=process.env.OPENROUTER_MODEL_OPTIONS||'';const models=raw.split(',').map(x=>x.trim()).filter(Boolean).map(id=>({id,label:id}));res.status(200).json({models,default:process.env.OPENROUTER_MODEL||'openai/gpt-4.1-mini'})}